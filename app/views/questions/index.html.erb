<h1>Questions#index</h1>
<p>Find me in app/views/questions/index.html.erb</p>
<% u = @current_user %>
<strong>
	<p>Preguntas de Jakintero</p>
</strong>
<p>
	Question title:
	<%= u.questions.last.title %>
</p>
<p>
	Question content:
	<%= u.questions.last.content %>
</p>
<p>
	Answer:
	<%= u.questions.last.answers.last.content %>
</p>
<p>
	Comment:
	<%= u.questions.last.answers.first.comments.last.content %>
</p>
<p>
	Votes:
	<%= u.questions.last.answers.first.votes.last.value %>
</p>



<strong>
<ul> Preguntas de todos los usuarios: <br><br>
</strong>
	<% @questions.each do |question| %>

		<p> Título:
			<%= question.title %>
		</p>
		<h4> Contenido:
		<%= question.content %>
		</h4>
	<% end %>
</ul>

<% if user_signed_in? %>
  <a rel="nofollow" data-method="delete" href="/users/sign_out">
  Salir <span class="sr-only"></span></a>
<% end %>
<br>
<%= link_to "Create", new_question_path %>


